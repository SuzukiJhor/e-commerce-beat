import HeroSlider from "../components/sliders/HeroSlider";

const Home = () => {
  return (
    <main>
      <section id="hero">
        <HeroSlider />
      </section>

      <section></section>

      <section></section>
    </main>
  );
};

export default Home;
